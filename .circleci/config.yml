version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.15-browsers
    steps:
      - checkout
      - run: yarn
      - run: yarn build
  test:
    docker:
      - image: circleci/node:8.15-browsers
    steps:
      - checkout
      - run: yarn
      - run: yarn test
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test